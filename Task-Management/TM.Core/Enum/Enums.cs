namespace TM.Core.Enum
{
    public enum Priority
    {
        High = 0,
        Medium = 1,
        Low = 2
    }

    public enum CurrentTaskStatus
    {
        Pending = 0,
        InProgress = 1,
        Completed = 2
    }
}
